# Графика — pygame. У апстрим-pygame нет колёс (wheel) под Python 3.14+,
# поэтому там ставим pygame-ce: это поддерживаемый форк, ставится как
# `import pygame` (полностью совместим по коду). На 3.13 и ниже — обычный pygame.
pygame>=2.6; python_version < "3.14"
pygame-ce>=2.5; python_version >= "3.14"
